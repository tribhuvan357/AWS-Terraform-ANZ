apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    codefresh.io/entity: internal-config
    codefresh.io/internal: 'true'
  name: in-cluster
spec:
  destination:
    namespace: codefresh-minikube
    server: https://kubernetes.default.svc
  project: cf-root-app-project
  source:
    directory:
      include: >-
        {all-runtimes-all-clusters/**/*.yaml,all-runtimes-all-clusters/*.yaml,codefresh-minikube/cf-gitops-runtime.yaml,codefresh-minikube/default-git-source.yaml,control-planes/**/*.yaml,control-planes/*.yaml,runtimes/codefresh-minikube/**/*.yaml,runtimes/codefresh-minikube/*.yaml}
      recurse: true
    path: resources
    repoURL: https://github.com/tribhuvan357/AWS-Terraform-ANZ.git
    targetRevision: HEAD
  syncPolicy:
    automated:
      allowEmpty: true
      prune: true
      selfHeal: true
    syncOptions:
      - allowEmpty=true
